# Generative-AI-chatbot-friendly-project-text-generator
A script that starts from current working directory, finds all files with extensions such as .txt, .json, etc; copies the text, and the entire directory structure, and saves it into repo_dump.txt. Can be used to enter as input to generative AI such as ChatGPT.
